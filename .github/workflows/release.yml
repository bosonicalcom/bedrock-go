name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    uses: bosonicalcom/github-workflows/.github/workflows/release-lib-go.yml@master
    with:
      go-version: "1.26.1"
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
